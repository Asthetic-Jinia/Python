
num = float(input("Enter a number: "))
z = float(input("Enter the divisor (z): "))

# Calculate the remainder
remainder = num % z

print("The remainder when", num, "is divided by", z, "is:", remainder)